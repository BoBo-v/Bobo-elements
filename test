 ---
  1. 组件导出不完整（高优先级）

  目前 src/index.ts 只通过 src/vue/index.ts 和 src/react/index.ts 导出了 Button 和 Icon，但实际已实现了 10+ 组件（Select、Form、Message 等）都没有正式导出。用户安装后无法按需引入这些组件。

  建议： 在 src/vue/index.ts 中补齐所有 Vue 组件的导出，包含 install 方法。

  ---
  2. React 组件覆盖不全（高优先级）

  React 侧目前只有 Button、Alert、Collapse、Dropdown、Icon、Tooltip，缺少：
  - Select、Form/FormItem、Input、Message、Notification、Switch

  建议： 利用 src/core/ 中已有的共享逻辑（hooks、types），逐步补齐 React 组件实现。

  ---
  3. 测试覆盖率不足（高优先级）

  目前仅约 13 个测试文件，Select、Form、Dropdown、Message、Notification、Switch 等组件没有测试。

  建议：
  - 为每个组件补充单元测试
  - 配置覆盖率报告（vitest 内置 --coverage）
  - 考虑引入 Playwright/Cypress 做 E2E 测试

  ---
  4. 依赖版本过旧（中优先级）
  ┌────────────┬──────────────┬────────────┐
  │    依赖    │   当前版本   │    最新    │
  ├────────────┼──────────────┼────────────┤
  │ Vue        │ 3.3.4        │ 3.5+       │
  ├────────────┼──────────────┼────────────┤
  │ React      │ 18.2.0       │ 19.x       │
  ├────────────┼──────────────┼────────────┤
  │ Vite       │ 4.0          │ 6.x        │
  ├────────────┼──────────────┼────────────┤
  │ TypeScript │ 4.7.4        │ 5.x        │
  ├────────────┼──────────────┼────────────┤
  │ VitePress  │ 1.0-alpha.65 │ 1.x 正式版 │
  ├────────────┼──────────────┼────────────┤
  │ Vitest     │ 0.29.8       │ 2.x        │
  └────────────┴──────────────┴────────────┘
  建议： 逐步升级核心依赖，特别是 TypeScript 5 和 Vite 5/6 带来的性能和 DX 提升。

  ---
  5. 构建与发布优化（中优先级）

  - 缺少 CSS 按需引入：当前只有一个全量 index.css，无法 tree-shaking 样式
  - vue 和 react 放在同一个包里，用户即使只用 Vue 也要装 React 的 peer dependency

  建议：
  - 每个组件单独输出 CSS 文件，支持按需加载
  - 考虑拆分为 @bobocn/element-vue 和 @bobocn/element-react 两个包，共享 @bobocn/element-core

  ---
  6. 文档需要完善（中优先级）

  - 文档内容偏简单，缺少 Props/Events/Slots 的 API 表格
  - 缺少 Playground 在线编辑能力
  - 无 Changelog

  建议： 为每个组件文档补充标准的 API 表格（属性、事件、插槽、方法），参考 Element Plus 的文档格式。

  ---
  7. 工程化完善（低优先级）

  - 无 CI/CD：建议配置 GitHub Actions（lint、test、build、自动发布）
  - 无 Monorepo：如果要拆包，可引入 pnpm workspace + turborepo
  - 无 Changelog 自动生成：可用 changesets 或 conventional-changelog
  - 缺少 Storybook：方便组件独立开发调试

  ---
  8. 组件能力扩展（低优先级）

  可考虑补充常见组件：
  - 布局类： Layout（Row/Col）、Space、Divider
  - 数据展示： Table、Tag、Badge、Card、Tabs
  - 反馈类： Dialog/Modal、Drawer、Loading
  - 导航类： Menu、Breadcrumb、Pagination

  ---
  总结优先级排序
  ┌────────┬───────────────────┬────────────────────────────┐
  │ 优先级 │       事项        │          预期收益          │
  ├────────┼───────────────────┼────────────────────────────┤
  │ P0     │ 补齐组件导出      │ 让已实现的组件能被用户使用 │
  ├────────┼───────────────────┼────────────────────────────┤
  │ P0     │ 补充测试覆盖      │ 保证代码质量和重构信心     │
  ├────────┼───────────────────┼────────────────────────────┤
  │ P1     │ 补齐 React 组件   │ 兑现双框架承诺             │
  ├────────┼───────────────────┼────────────────────────────┤
  │ P1     │ 升级核心依赖      │ 安全性 + 新特性            │
  ├────────┼───────────────────┼────────────────────────────┤
  │ P1     │ CSS 按需引入      │ 减小用户打包体积           │
  ├────────┼───────────────────┼────────────────────────────┤
  │ P2     │ 完善文档 API 表格 │ 改善开发者体验             │
  ├────────┼───────────────────┼────────────────────────────┤
  │ P2     │ 配置 CI/CD        │ 工程化规范                 │
  ├────────┼───────────────────┼────────────────────────────┤
  │ P3     │ 拆分 Vue/React 包 │ 减少不必要的依赖           │
  ├────────┼───────────────────┼────────────────────────────┤
  │ P3     │ 新增更多组件      │ 扩大组件库覆盖面           │
  └────────┴───────────────────┴────────────────────────────┘